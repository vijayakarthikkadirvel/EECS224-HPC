PARALLEL MERGE SORT USING OPENMP

I used classical parallel merge sort algorithm which improves the time to solve when compared to the sequential algorithm by recursion.

To run it:
1.) make 
2.) run the script in HPC cluster 
3.) Check the results after the execution is over in the HPC cluster.
